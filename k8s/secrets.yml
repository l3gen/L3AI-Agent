apiVersion: v1
kind: Secret
metadata:
  name: levatas-config
  namespace: levatas
type: Opaque
stringData:
  CONFIDENCE_THRESHOLD: "0.70"
  AI_SERVICE_URL: "http://ai-service:5001"
  REVIEW_SERVICE_URL: "http://review-service:5002"
  # Add real secrets here — never commit actual values
  # Use: kubectl create secret generic levatas-config --from-env-file=.env -n levatas
